Library Link : export LD_PRELOAD=$(realpath ./libc-2.27.so)
p = process("./executefile", env= {"LD_PRELOAD" : "./libc-2.27.so"})

# For docker file

cd ~

apt-get -y update && apt-get -y upgrade

apt-get install -y vim gdb git python3 python3-pip python3-dev libssl-dev libffi-dev build-essential ruby gcc-multilib binutils

python3 -m pip install --upgrade pip && python3 -m pip install --upgrade pwntools

gem install one_gadget

git clone https://github.com/Gallopsled/pwntools.git && git clone https://github.com/JonathanSalwan/ROPgadget.git && git clone https://github.com/scwuaptx/Pwngdb.git && git clone https://github.com/longld/peda.git

cp ./Pwngdb/.gdbinit .
echo "source ~/peda/peda.py" >> ~/.gdbinit


